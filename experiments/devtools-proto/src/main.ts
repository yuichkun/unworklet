// Demo app entry. Deliberately empty — the prototype's purpose is to make
// the unworklet plugin contribute panels to Vite DevTools so we can eyeball
// the UX. No actual unworklet processor runs here.

console.log("[unworklet devtools proto] demo app booted");
