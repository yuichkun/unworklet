{
  "name": "unworklet-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp test && vp run --filter ./examples/01-stereo-gain test:e2e",
    "ready": "vp check && vp run test && vp run -r build"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.3.0"
}
