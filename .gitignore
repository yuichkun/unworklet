# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local
*.wav
!**/__snapshots__/*.wav

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent worktrees
.claude/worktrees/
.claude/scheduled_tasks.lock

# Playwright
test-results/
playwright-report/

# Vitest browser-mode run artifacts (failure screenshots / attachments)
**/.vitest-attachments/
**/__screenshots__/

# Vite+ git hooks (generated locally by `vp config` after clone)
.vite-hooks/

# Transient lowered-`.uwk.ts` temp the vite-plugin build path writes then removes
*.uwklowered.ts

# Vercel CLI project link (local, per-machine)
.vercel
