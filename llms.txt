# unworklet

> unworklet compiles declarative TypeScript into realtime-safe WebAssembly that
> runs on any audio thread — browser, server, or microcontroller. You compose a
> processor from typed, reusable primitives (audio, params, state, events, MIDI);
> the compiler proves it is allocation-free, GC-free, and bounded, then emits the
> WASM, the AudioWorkletProcessor glue, and a typed main-thread node. Effects,
> synths, samplers, MIDI processors — built from the same primitives.

This file orients an AI agent that has installed unworklet. **The source code is
the source of truth.** The call forms below are exact — do not substitute forms
from memory. When in doubt, read the package READMEs in `node_modules` (e.g.
`@unworklet/core`'s) or `docs/12-canonical-examples.md` in the repo.

## Packages (install what you use)

- **@unworklet/core** — the DSL + compiler + main-thread client. Always needed.
- **@unworklet/unplugin** (dev dep) — load processors via `?worklet`; DevTools.
- **@unworklet/lang** — optional `.uwk.ts` sugar (`a * b` instead of `a.mul(b)`).
- **@unworklet/offline** — render a processor to PCM in Node/Bun/Deno.
- **@unworklet/test** — audio/event/MIDI assertions for Vitest.

A processor is authored in `name.processor.ts` (or `name.uwk.ts`) and imported
in the app with a **default** import: `import x from "./name.processor.ts?worklet"`.
Type the `?worklet` import once per project by extending the tsconfig the plugin
generates: `{ "extends": "./.unworklet/tsconfig.json" }` (no `vite-env.d.ts`). The
plugin writes `.unworklet/` on dev/build — the tsconfig plus a `worklets.d.ts` of
per-processor types — so `node.params.<name>` (and state / events / midi / inputs /
outputs) are typed and an undeclared name errors. Don't add your own `include` to
that tsconfig (`extends` doesn't merge it). Can't extend? Write the three settings
directly: `compilerOptions.types: ["@unworklet/unplugin/client"]`,
`compilerOptions.plugins: [{ name: "@unworklet/lang/typescript-plugin" }]`, and list
`.unworklet/worklets.d.ts` in `include` (a glob skips the dot-folder). `.unworklet/`
is a generated artifact — gitignore it.

## DSL quick reference (exact forms)

Declarations go at the top of `defineProcessor((ctx) => { ... })`; the returned
`process` runs every 128-sample block. `ctx.sampleRate` is the host sample rate —
use it for anything rate-dependent, e.g. an oscillator's phase step
(`const step = 440 / ctx.sampleRate`) or a filter coefficient.

```ts
import { audioInput, audioOutput, defineProcessor, forSample, param, state, event } from "@unworklet/core";

audioInput({ channels, name })       // read: in.left.at(i) / in.ch(c).at(i)
audioOutput({ channels, name })      // write: out.left.at(i).write(v) / out.ch(c).at(i).write(v)
param.f32({ default, min, max, automationRate: "a-rate" | "k-rate" }).named("g")  // read: g.at(i)
state.f32(0).named("s")              // also .f64/.i32/.i64/.bool — read s.read(), write s.write(v)
state.buffer.f32({ size }).named("b")    // buf.read(i), buf.write(i, v), buf.readInterpolated(pos)
event({ to: "main", name })          // worklet→main: .emitIf(cond, payload)
event({ from: "main", name })        // main→worklet: .onReceive(p => ...)
event.midi({ from: "main", name })   // inbound MIDI: .onEvent("noteOn", e => ...)
event.midi({ to: "main", name })     // outbound MIDI: .emitIf(cond, midiEvent)
defineSubgraph((args) => ({ run: (x) => ... }))  // reusable block; instantiate(sg, ...args, { name }) in decl scope ($prev feedback is .uwk.ts-only)
// A subgraph can live in its own file: a .uwk.ts with no process() is a library module that exports defineSubgraph(...);
// a processor imports it with a normal `import { sg } from "./sg.uwk.ts"` (not ?worklet). Publishing one: @unworklet/core as a peerDependency.

forSample((i) => { /* i = 0..127 */ })          // the per-sample loop
forSample.byN(4, (i) => { ... })                // SIMD stride (divides 128)
// SIMD ops are an opt-in subpath: import { vec4, splat, addVec, mulVec, sumLanes } from "@unworklet/core/simd"
//   v = buf.loadVec(i);  v.lane(0..3);  buf.storeVec(i, v)   (f32 buffers; .ts only)

// operations — free function OR method form, identical:
add sub mul div mod neg  eq lt lte gt gte  not select  abs min max clamp
floor ceil frac  sin cos tan tanh exp log sqrt  pipe
// scalar constructors AND casts: f32(x) f64(x) i32(x) i64(1n) bool(true)
//   f32(node) reinterprets any scalar Node to Node<"f32"> (the i32→f32 bridge, e.g. a MIDI field)
```

Naming: `.named("x")` or `.expose({ name, snapshot, publish: { rateFps } })`.
`publish` streams to `node.state.<name>.subscribe(...)`. A named scalar `state` is
**persistent by default** (in `node.snapshot()` / offline `r.state`); `snapshot:"transient"`
opts it out, and buffers are the reverse (transient unless `snapshot:"persistent"`). A
`publish`/`persistent` slot must be named.
Migrate persisted state across schema changes with the 2nd `defineProcessor` arg:
`defineProcessor(body, { migrations: [{ from, to, migrate(blob, h) { ... } }] })` (`migrate` is sync).

## Do NOT hallucinate these (common mistakes)

- State read/write is `s.read()` / `s.write(v)` — **not** `.load()/.store()`, **not** `.get()/.set()`.
- Audio I/O and params index with `.at(i)`; buffers use `.read(i)` / `.write(i, v)`.
- Ports require a `name`: `audioInput({ channels: 2, name: "main" })` — never positional.
- There is **no** standalone `message<T>()`; main→worklet is `event({ from: "main", name })`.
- MIDI is `event.midi({ from | to: "main", name })`, not a `midiInput()/midiOutput()` call.
- An inbound-MIDI handler — `keys.onEvent("noteOn", e => ...)` — gives `e.note` / `e.velocity` / `e.channel` / `e.atSample` as **`Node<"i32">`**, not JS numbers. Cast into float math with `f32(e.note)`, and carry a handler value into `process` by writing it to `state` (the handler runs on message arrival; `forSample` runs per block).
- A worklet→main `event` emits only via `.emitIf(cond, payload)` — there is no bare `.emit()` on the worklet side. To fire once on a note, set a flag `state` in the handler and `.emitIf(flag.read(), …)` in `process`.
- Infix operators (`a * b`, `a + b`) on `Node`s work ONLY in `.uwk.ts`. In `.ts` / `.processor.ts` use methods or free functions (`a.mul(b)` / `mul(a, b)`) — a `Node` has no JS operators.
- Import processors with the `?worklet` query as the **default** import (`import x from "...?worklet"`) — not a named import, and don't import the raw module.
- `.uwk.ts` sugar and the plain `.ts` API are not mixed in one file. Without the `@unworklet/lang` editor plugin a `.uwk.ts` needs `// @ts-nocheck`; the `{ "extends": "./.unworklet/tsconfig.json" }` setup above already includes that plugin (it auto-injects the shipped ambient — no `files` / `types` entry), so the sugar type-checks and the header is dropped (see `@unworklet/lang`'s README → IDE support).

## Main-thread usage

```ts
import { createNode } from "@unworklet/core";
import stereoGain from "./stereo-gain.processor.ts?worklet";
const node = await createNode(ctx, stereoGain);
node.outputs.main.connect(ctx.destination);   // also: node.inputs, node.params.<name>.value,
                                               // node.state.<name>.subscribe(cb),
                                               // node.events.<name>.on(cb) / .emit(payload),   // .emit = main→worklet send
                                               // node.midi.<name>.send(ev) / .onEvent("noteOn", e => ...),
                                               // await node.snapshot() / await node.restore(blob)   // both async
```

## Verify before you ship

```ts
import { renderOffline } from "@unworklet/offline";
import { expectNoNaN, expectStable } from "@unworklet/test";
const r = await renderOffline(stereoGain, { sampleRate: 48000, duration: 1, inputs: { main: [pcm] },
  params: { g: [0.5] }, messages: [{ name, payload }], events: [{ name, payload, atSample }] });
  // events = inbound MIDI: name = the event.midi({from:"main"}) port name, payload = a midi.noteOn(...) from @unworklet/test
expectNoNaN(r); expectStable(r);   // r.outputs, r.events (worklet→main + outbound MIDI), r.state (snapshot blob)
```

## More

- Full DSL + examples: `docs/12-canonical-examples.md`, `docs/01-dsl.md`.
- The unworklet Skill (`.claude/skills/unworklet/SKILL.md`) carries the same
  reference for Claude/agent tooling.
